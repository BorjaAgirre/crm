<form method="post" action="{{ path('app_colectivo_delete', {'id': colectivo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ colectivo.id) }}">
    <button class="btn">Delete</button>
</form>
