{% extends 'base.html.twig' %}

{% block title %}Tiporelacion index{% endblock %}

{% block body %}
    <h1>Tiporelacion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tipo</th>
                <th>Tipocontrario</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tiporelacion in tiporelacions %}
            <tr>
                <td>{{ tiporelacion.id }}</td>
                <td>{{ tiporelacion.Tipo }}</td>
                <td>{{ tiporelacion.Tipocontrario }}</td>
                <td>
                    <a href="{{ path('app_tiporelacion_show', {'id': tiporelacion.id}) }}">show</a>
                    <a href="{{ path('app_tiporelacion_edit', {'id': tiporelacion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tiporelacion_new') }}">Create new</a>
{% endblock %}
