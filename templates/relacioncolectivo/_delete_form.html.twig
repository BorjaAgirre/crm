<form method="post" action="{{ path('app_relacioncolectivo_delete', {'id': relacioncolectivo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relacioncolectivo.id) }}">
    <button class="btn">Delete</button>
</form>
